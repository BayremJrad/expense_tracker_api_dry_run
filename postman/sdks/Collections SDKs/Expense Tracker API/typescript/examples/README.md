# expense-tracker-api-sdk

A basic example of how to use the expense-tracker-api-sdk package.

## Installation

In the event `expense-tracker-api-sdk` is not published to npm, you can install it locally by running the following command in the examples folder:

```sh
npm run setup
```

This will rebuild the parent package and install it locally.

Otherwise you can install it from npm:

```sh
npm install expense-tracker-api-sdk
```

## Usage

To run the example, run the following command in the examples folder:

```sh
npm run start
```
